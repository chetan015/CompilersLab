#include <stdio.h>
#include <stdlib.h>
void main()
{
	float array[5];
	int global = array[1];
	int i,j;
	scanf(i);
	while(i<=10)
	{
		sum(i,global);
		i = i + 1;
	}
	if(global!=20)
		global = 20;
	for(j=10;j!=0;j=j-1)
	{
		printf(j);
	}
}

		

